# Gemma-2 QLoRA training package

This Kaggle package runs the canonical project notebook with the full
competition training file and the evaluation rule `id % 5 == 0`.

The notebook is mirrored here because Kaggle kernel packages require the code
file and `kernel-metadata.json` to live in one directory. The canonical,
readable copy is maintained under `notebooks/`.
